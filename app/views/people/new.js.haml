%h3 Add new person
= simple_form_for(:person, :url => people_path, :remote => true) do |f|
  = f.input :name
  = f.input :birthdate, :as => :date, :start_year => 1900, :end_year => 2011
  = f.input :imdb_id
  = f.button :submit
